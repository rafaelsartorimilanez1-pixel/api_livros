export interface BookModel {
    id: number,
    title: string,
    author: string,
    genre: string,
    publisher: string,
    publication_year: number,
    pages: number,
    language: string,
    isbn: string,
    available: boolean
}